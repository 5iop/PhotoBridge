version: '3.8'

services:
  photobridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: photobridge
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - API_KEY=${API_KEY:-photobridge-api-key}
      - JWT_SECRET=${JWT_SECRET:-photobridge-jwt-secret}
      - PORT=80
      - GIN_MODE=release
      - CNCDN_URL=${CNCDN_URL:-}
      - THUMB_WORKERS=${THUMB_WORKERS:-2}
      - THUMB_JOB_TIMEOUT_SECONDS=${THUMB_JOB_TIMEOUT_SECONDS:-120}
    volumes:
      - photobridge_data:/app/data
      - photobridge_uploads:/app/uploads
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/api/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

volumes:
  photobridge_data:
  photobridge_uploads:
